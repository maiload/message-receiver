spring:
  application:
    name: orchestrator

  datasource:
    url: jdbc:postgresql://localhost:5432/message_receiver
    username: maiload
    password: maiload

  kafka:
    bootstrap-servers: localhost:9094
    producer:
      acks: all
      retries: 3

server:
  port: 8084

orchestrator:
  minio:
    endpoint: http://localhost:9000
    access-key: maiload
    secret-key: maiload123
    bucket: message-receiver
  kafka:
    bulk-task-topic: bulk.send.task
  job:
    poll-interval-ms: 5000
    chunk-size: 10000
